{
  "name": "zetgui-desktop",
  "version": "1.0.0",
  "description": "ZetGui Desktop Application",
  "main": "index.js",
  "scripts": {
    "start": "./start.sh",
    "dev": "WEBKIT_DISABLE_COMPOSITING_MODE=1 GDK_BACKEND=x11 npx neu run",
    "build": "cd react-src && npm run build && cp -r build/* ../www/",
    "setup": "npm install && cd react-src && npm install",
    "postinstall": "neu update || echo 'Neutralino binary will be downloaded on first run'"
  },
  "keywords": ["neutralino", "desktop", "gui"],
  "author": "derx",
  "license": "MIT",
  "devDependencies": {
    "@neutralinojs/neu": "^11.4.0"
  }
} 